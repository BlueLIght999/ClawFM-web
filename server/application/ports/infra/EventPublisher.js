/**
 * @typedef {object} EventPublisher
 * @property {(event: string, payload?: unknown) => void} emit
 * @property {(socketId: string, event: string, payload?: unknown) => void} toClient
 */

export {};
